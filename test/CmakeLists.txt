# find test files
set(LIBJOT_TEST_HOOK_FILES test_hook.cc)
set(LIBJOT_TEST_TIME_FILES test_time.cc)

add_executable(LIBJOT_TEST_HOOK ${LIBJOT_TEST_HOOK_FILES})
add_executable(LIBJOT_TEST_TIME ${LIBJOT_TEST_TIME_FILES})

target_link_libraries(LIBJOT_TEST_HOOK LIBJOT_SHARED)
target_link_libraries(LIBJOT_TEST_TIME LIBJOT_SHARED)

#file(COPY "${CMAKE_BINARY_DIR}/bin/*.dll" DESTINATION "${CMAKE_BINARY_DIR}/test")